#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    int n,k ; cin >> n;
    int a[n];
    for(int i = 0; i < n ; i++){
        cin >> a[i];
    }
    cin >> k;
    for(int x : a){
        if( x != k) cout << x << " ";
    }
}
